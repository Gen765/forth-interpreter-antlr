# Third-party notices

The grammar, interpreter and generated Python sources are authored by Genís
Verge Martín. Parser generation and runtime support use ANTLR 4.13.2, developed
by the ANTLR project and distributed under its own BSD 3-Clause licence.

`antlr4-python3-runtime` is installed from PyPI and is not vendored here. The
optional `antlr4-tools` launcher is used only by maintainers to regenerate the
versioned lexer, parser and visitor.
